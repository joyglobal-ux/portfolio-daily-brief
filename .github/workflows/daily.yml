name: Daily Brief

on:
  # 매일 22:00 UTC (= 다음날 07:00 KST). cron 6개 별도 필드 주의 (분 시 일 월 요일).
  schedule:
    - cron: "0 22 * * *"
  # 수동 실행 가능
  workflow_dispatch:

permissions:
  contents: write  # output/ 커밋용
  pages: write
  id-token: write

concurrency:
  group: daily-brief
  cancel-in-progress: false

# Node.js 24로 강제 실행 (2026-06-02 default 전환 대비)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Generate brief
        env:
          GOOGLE_SHEETS_CSV_URL: ${{ secrets.GOOGLE_SHEETS_CSV_URL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
          CLAUDE_MODEL: ${{ vars.CLAUDE_MODEL || 'claude-sonnet-4-5-20250929' }}
        run: uv run python -m scripts.main

      - name: Commit output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/
          if git diff --staged --quiet; then
            echo "변경 사항 없음."
          else
            git commit -m "chore: daily brief $(date -u +%Y-%m-%d)"
            git push
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: output/

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
